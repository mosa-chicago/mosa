[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[context.deploy-preview]
  command = "npm ci && npm run build"
  publish = "dist"

[context.branch-deploy]
  command = "npm ci && npm run build"
  publish = "dist"
